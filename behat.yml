default:
    suites:
        dummy-client:
            paths: [ %paths.base%/features ]
            contexts:
                - FeatureContext:
                    client: "dummy"

        guzzle-client:
            paths: [ %paths.base%/features ]
            contexts:
                - FeatureContext:
                    client: "guzzle"
